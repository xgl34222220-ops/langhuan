# 琅嬛 DESIGN.md

琅嬛的视觉目标不是做成“AI 工具面板”，而是先成为一款安静、精致、耐看的中文阅读器；AI 创作、进入故事、角色接管和世界模拟只在用户需要时出现。

设计参考：
- bergside/awesome-design-skills：Editorial / Clean / Glassmorphism 等设计技能的结构化规则。
- VoltAgent/awesome-design-md：Apple 等品牌设计分析中“内容优先、界面克制、单一强调色、UI chrome 后退”的方法。

这里只吸收设计原则，不复制品牌视觉、字体或商标。

## 1. 设计气质

关键词：**阅读优先、东方克制、现代、轻盈、精致、低噪声、流动层次**。

- 正文和书籍封面永远是视觉主体。
- 大面积背景保持安静，避免铺满渐变、发光、描边和装饰图形。
- 毛玻璃 / 液态玻璃只用于“浮在内容上方”的控制层：顶部工具条、底部阅读控制、Bottom Sheet、短暂状态层。
- 不把所有卡片都做成玻璃；内容卡片优先使用纯色 Surface + 极轻层级差。
- 同一个页面只允许一个主要强调色，由 Material 动态色 / 当前主题提供，不自行堆多套品牌色。
- 阴影只用于真实层级：封面、浮动控制、弹层。普通列表和正文不加阴影。

## 2. 布局与间距

采用 8dp 为主节奏，同时允许 4dp 微调。

- 页面左右安全边距：18–24dp。
- 主要区块间距：24–32dp。
- 卡片内部：16–20dp。
- 列表行垂直触控区：至少 48dp。
- 图标按钮最小触控尺寸：48dp；紧凑场景不得低于 44dp。
- 封面圆角：12–18dp。
- 普通卡片：18–24dp。
- 浮动控制 / Bottom Sheet 主卡：24–30dp。
- 主按钮优先胶囊或大圆角，避免小方按钮。

## 3. 字体层级

Android 端优先系统字体和 ROM 原生中文字体，不内置来路不明的字体文件。

建议层级：
- 页面标题：28–32sp / Bold–Black。
- 作品标题：24–28sp / Bold。
- 章节标题：24–28sp / Bold。
- 正文默认：18–20sp / Regular。
- 正文行高：1.70–1.95 × 字号。
- 普通正文 UI：15–17sp。
- 辅助信息：12–14sp。

规则：
- 书名、章节名最多使用 2 行，超出省略。
- 正文不使用极粗字重。
- 阅读页正文区域不混入多种字号和多种颜色。

## 4. 色彩与层级

全部优先使用 `MaterialTheme.colorScheme` 语义色，不在页面散落固定 RGB。

层级建议：
1. `surface`：正文与主背景。
2. `surfaceContainerLow`：轻量功能分区。
3. `surfaceContainer` / `surfaceContainerHigh`：浮层、工具层。
4. `primary`：唯一主要强调色。
5. `onSurfaceVariant`：次级信息。

玻璃层使用主题 Surface 的半透明版本，但必须保持文字对比度；透明不能以牺牲可读性为代价。

## 5. 书架

书架是 App 第一印象，必须像阅读器，不像后台管理系统。

### 必须
- 顶部只保留：品牌标题 / 搜索 / 布局切换 / 添加 / 更多。
- 书籍是视觉主体，网格模式封面面积应明显大于文字面积。
- 本地导入状态只作为轻量标签，不抢封面。
- 空书架只保留一个主行动“导入本地书籍”，AI 新建作为次级行动。
- 添加书籍通过 Bottom Sheet 展开，本地导入为第一项。

### 禁止
- 首页堆 AI Provider、任务统计、技能状态、运行日志等后台信息。
- 每本书卡片塞 4–6 个按钮。
- 书架同时出现多个强 CTA。

## 6. 作品详情

作品详情应像“拿起一本书后的封面内页”。

优先级：
1. 封面
2. 书名
3. 章节数 / 字数 / 来源
4. 开始或继续阅读
5. 进入故事
6. 目录 / 创作 / AI 等工具

AI 入口存在，但不能比“阅读”更醒目。

## 7. 阅读页

阅读页遵循 **content first / chrome recedes**。

- 默认正文占据绝大多数屏幕。
- 点击正文区域可隐藏 / 显示顶部和底部控制。
- 顶部控制只显示返回、书名、目录、阅读设置、必要操作。
- 底部控制只显示章节进度、上一章/下一章、进入故事。
- 阅读设置通过 Bottom Sheet 提供字号、行距、页边距；后续可增加主题、字体、亮度。
- 阅读控制出现时应像浮在正文上方的轻量层，而不是把正文切成多个面板。
- 正文选择复制能力保留。

## 8. “进入故事”视觉规则

“进入故事”是琅嬛的特色，但必须自然。

- 在作品详情中作为次主按钮。
- 在阅读页底部作为当前章节的明确动作。
- 不在正文中频繁插入 AI 卡片或悬浮气泡。
- 进入故事后可以使用更强的层次和状态反馈，但仍保持同一套圆角、间距、颜色语义。

## 9. 动效

动效只用于解释状态和空间关系。

允许：
- Bottom Sheet 展开 / 收起。
- 控制栏淡入淡出。
- 页面层级切换的轻量过渡。
- 加载进度和成功反馈。

禁止：
- 无意义呼吸光。
- 大面积持续流动渐变。
- 卡片不停漂浮。
- 为了“高级感”增加影响滑动性能的装饰动画。

## 10. 空状态 / 加载 / 错误

每个一级功能必须有完整状态：
- Empty：告诉用户下一步能做什么。
- Loading：说明正在处理什么文件 / 章节 / AI 任务。
- Error：说清失败原因，并提供可执行动作。
- Success：尽量短，不挡住阅读路径。

错误文案禁止只写“失败”“未知错误”。

## 11. 可访问性与手机适配

- 重要触控区域 >= 44dp，目标 48dp。
- 文字对比保持可读，透明层尤其注意。
- 大字号下按钮与标题允许增高，不强行裁切。
- 长书名、长章节名必须可省略或换行。
- 支持浅色 / 深色 / 动态色，不假设固定白底黑字。
- UI 不依赖 hover 才能发现关键功能。

## 12. 代码审查 QA

每次改 UI 前后检查：

- [ ] 第一眼能否看出这是阅读器，而不是 AI 控制台？
- [ ] 页面是否只有一个最强主操作？
- [ ] 正文 / 封面是否比工具按钮更抢眼？
- [ ] 是否遵循统一 8dp 节奏和统一圆角？
- [ ] 是否把玻璃效果限制在真正的浮层？
- [ ] 是否存在低对比度透明文字？
- [ ] 所有主要按钮是否至少 44dp 高？
- [ ] 空 / 加载 / 错误状态是否完整？
- [ ] 是否存在没有功能意义的动画、渐变、阴影？
- [ ] 深色模式和动态色下是否仍然成立？
- [ ] 长书名、长章节名、超长本地小说是否不会破坏布局？

## 13. 当前 Reader-first 迁移

当前优先改造顺序：
1. 本地书架。
2. 本地 TXT / EPUB / Markdown 导入。
3. 作品详情。
4. 沉浸阅读。
5. 从当前章节进入故事。
6. 阅读设置与阅读进度持久化。
7. 后续再统一创作页、故事页和 AI 配置页。
