package com.xiguli.langhuan.ui

import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

/** Temporary compatibility token used while ShelfMobileExperience V2 is being split into smaller UI primitives. */
internal object MaterialTypography {
    val labelMedium = TextStyle(fontSize = 12.sp, lineHeight = 17.sp, fontWeight = FontWeight.Medium)
}
