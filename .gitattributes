# 仓库内一律 LF。之前 Windows 侧的编辑器把 296 个文件改写成了 CRLF，
# 导致 git status 常年 296 个「改动」、每个 diff 都是整文件重写，
# 真正的改动被淹没在行尾噪声里。
* text=auto eol=lf

# 二进制，别碰
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ttf binary
*.otf binary
*.jar binary
*.keystore binary
*.apk binary
